# OS / editor state
.DS_Store
.claude/
CLAUDE.md

# App data + per-deployment config
places.json
routes.json
catalog.local.json
.catalog.lock
gpx
users/
site-config.json
tools/config.json
tools/*.db*

# Docker Compose bind mount (compose.yml -> ./data:/data)
data/

# Compose environment overrides (secrets / per-deployment values)
.env
